<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/unitCardItem"
    android:layout_width="match_parent"
    android:layout_height="76dp"
    android:layout_margin="8dp"
    android:background="@drawable/shape_gray_box"
    android:clickable="true"
    android:elevation="2dp"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackgroundBorderless">

    <TextView
        android:id="@+id/unitSymbol"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_margin="8dp"
        android:background="@drawable/shape_gray_box"
        android:elevation="3dp"
        android:gravity="center"
        android:maxLength="4"
        android:paddingHorizontal="8dp"
        android:text="Q"
        android:textColor="@color/white"
        android:textSize="13dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/unitTitle"
        android:layout_width="0dp"
        android:layout_height="32dp"
        android:layout_margin="8dp"
        android:gravity="center_vertical"
        android:text="Unit"
        android:textColor="@color/white"
        android:textSize="12sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@+id/unitSymbol"
        app:layout_constraintEnd_toStartOf="@+id/unitConvertInput"
        app:layout_constraintStart_toEndOf="@+id/unitSymbol"
        app:layout_constraintTop_toTopOf="@+id/unitSymbol" />


    <TextView
        android:id="@+id/unitConvertInput"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_margin="8dp"
        android:gravity="center"
        android:text="0.0"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/unitTitle" />

</androidx.constraintlayout.widget.ConstraintLayout>