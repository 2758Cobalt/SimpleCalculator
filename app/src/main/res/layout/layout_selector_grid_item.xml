<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/selectorCard"
    style="@style/Widget.Material3.CardView.Elevated"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:focusable="true"
    android:clickable="true"
    android:layout_margin="8dp"
    app:cardElevation="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_gravity="center_horizontal|top">

        <ImageView
            android:id="@+id/selectorGridItemIcon"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_margin="8dp"
            android:layout_gravity="center"
            android:contentDescription="@string/selector_icon_description"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_weight" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/selectorGridItemTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="8dp"
            android:gravity="center"
            android:contentDescription="@string/selector_text_description"
            android:textAppearance="@style/TextAppearance.Material3.BodyLarge"
            android:text="@string/converter_weight"/>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>